# 图集行装箱机制：写入位置、行高动态与空白浪费分析

## 一、坐标系与总体排列

### 1.1 纹理坐标系

Alacritty 的 Atlas 采用 OpenGL 标准纹理坐标系（见 [atlas.rs:18-31](file:///d:/fz/0601/solo-dogfeeding/code/335-alacritty/alacritty/src/renderer/text/atlas.rs#L18-L31) 的 ASCII 图）：

```
                    (W=1024, H=1024)  ← 纹理右上角
  ┌─────┬─────┬─────┬─────┬─────┐
  │ 10  │     │     │     │     │   ← 第三行（最上面一行）
  ├─────┼─────┼─────┼─────┼─────┤
  │ 5   │ 6   │ 7   │ 8   │ 9   │   ← 第二行
  ├─────┼─────┼─────┼─────┴─────┤
  │ 1   │ 2   │ 3   │ 4         │   ← 第一行（最下面一行）
  └─────┴─────┴─────┴───────────┘
(0, 0)  ─── x →                    ← 纹理左下角（原点）
        ↑
        y
```

关键点：
- **原点 (0,0) 在左下角**，Y 轴向上
- **行从下往上堆积**：第一行在底部，新行往上加
- `row_baseline` 表示**当前行的底部 Y 坐标**（基线）

### 1.2 行结构三变量

| 变量 | 含义 | 初始值 |
|:---:|------|:---:|
| `row_baseline` | 当前行的底部 Y 坐标（基线） | 0 |
| `row_extent` | 当前行已用宽度（X 方向推进量） | 0 |
| `row_tallest` | 当前行已插入字形的最大高度 | 0 |

三者共同定义了当前行的"写入游标"状态。

---

## 二、行内字形写入位置

### 2.1 写入原点

[insert_inner()](file:///d:/fz/0601/solo-dogfeeding/code/335-alacritty/alacritty/src/renderer/text/atlas.rs#L147-L151) 前两行：

```rust
let offset_y = self.row_baseline;   // 字形底部 Y = 行基线
let offset_x = self.row_extent;     // 字形左边缘 X = 行已用宽度
```

字形位图通过 `glTexSubImage2D(offset_x, offset_y, width, height, ...)` 上传到纹理。

**行内对齐方式**：
- **X 方向**：左对齐，字形依次向右紧排
- **Y 方向**：底部对齐，所有字形的底边落在同一条 baseline 上，向上生长

```
       ┌──────────┐
       │          │   ← 高字形顶部
┌───┐  │          │
│   │  └──────────┘   ← 高字形底部 = baseline + height_high
│   │ ┌──┐            ← 矮字形顶部 = baseline + height_low
└───┘ └──┘            ← 矮字形底部 = baseline
baseline  ──────────────────────
```

所有字形底部对齐 `row_baseline`，顶部参差不齐。

### 2.2 行内推进

插入完成后更新状态（[atlas.rs:198-202](file:///d:/fz/0601/solo-dogfeeding/code/335-alacritty/alacritty/src/renderer/text/atlas.rs#L198-L202)）：

```rust
self.row_extent = offset_x + width;  // X 方向推进字形宽度

if height > self.row_tallest {       // Y 方向只更新最大值
    self.row_tallest = height;
}
```

X 方向：每次累加字形宽度，紧密排列，无空隙。

Y 方向：**只增不减**，单调递增。每次插入更高的字形就刷新 `row_tallest`，矮字形不影响行高。

---

## 三、空白浪费方向与分布

### 3.1 Y 方向浪费

由于底部对齐 + 行高取最大值，**空白浪费在矮字形的上方**（远离 baseline 的一侧）。

```
row_baseline + row_tallest ──────────── 行顶
                       │       ↑
                       │空白   │ 浪费高度 = row_tallest - h_i
                  ┌──┐ │       │
                  │  │ │       ↓
        ┌──────┐  └──┘ ───────  矮字形顶
        │      │        ↑
        │      │ 空白    │ 浪费高度 = h_high - h_low
        │      │        │
        └──────┘ ───────  高字形顶
baseline ───────────────  行底（所有字形底部对齐）
```

**单字形浪费高度** = `row_tallest - glyph_height`

**整行浪费面积** = `row_width × row_tallest - Σ(glyph_width_i × glyph_height_i)`

其中 `row_width = Σ glyph_width_i`（X 方向紧密排列，无浪费）。

### 3.2 插入顺序不影响总浪费量

行高最终等于最高字形的高度，与插入顺序无关：
- 先矮后高：行高逐渐被撑高，矮字形上方空白越来越大
- 先高后矮：行高一开始就到顶，矮字形上方空白固定

两种顺序最终的行高相同，浪费的**总量**相同，只是浪费的**分布**不同。

### 3.3 一行内最高字形的位置

最高字形可以出现在行内任何位置。由于行高是单调增长的：
- 如果最高字形在最左边 → 行高一开始就定了，后续字形都在已知行高内排列
- 如果最高字形在最右边 → 行高逐渐被撑高，左边的矮字形"意外地"获得了越来越多的上方空白

但 `room_in_row` 不检查行内剩余高度，只检查 `gh < H - B`（从基线到纹理顶部的总距离），所以插入顺序对"能不能插"没有影响。

---

## 四、行高对换行边界的影响

### 4.1 advance_row 的计算

[advance_row()](file:///d:/fz/0601/solo-dogfeeding/code/335-alacritty/alacritty/src/renderer/text/atlas.rs#L234-L244)：

```rust
let advance_to = self.row_baseline + self.row_tallest;  // 下一行基线 = 当前行顶部
if self.height - advance_to <= 0 {
    return Err(AtlasInsertError::Full);
}
self.row_baseline = advance_to;  // 新基线 = 旧行顶
self.row_extent = 0;
self.row_tallest = 0;
```

**核心等式**：

```
下一行 baseline = 当前行 baseline + 当前行 row_tallest
```

两行之间无间隙，上一行的顶就是下一行的底。

```
┌───────────────────────┐  ← 第二行顶 = baseline2 + tallest2
│  第二行               │
├───────────────────────┤  ← 第二行底 = 第一行顶 = baseline1 + tallest1 = baseline2
│  第一行               │
└───────────────────────┘  ← 第一行底 = baseline1 = 0
```

### 4.2 行高直接决定后续可用行数

行越高，后续行数越少。这是最简单的因果关系。

但更微妙的是：**room_in_row 的高度检查跟 row_tallest 无关**。

[room_in_row()](file:///d:/fz/0601/solo-dogfeeding/code/335-alacritty/alacritty/src/renderer/text/atlas.rs#L225-L231)：

```rust
let enough_height = raw.height < (self.height - self.row_baseline);
```

高度判定只看 `row_baseline`，**不看** `row_tallest`。这意味着：

- 只要字形高度 `< H - B`，不管当前行已经有多高（row_tallest 多大），都能插入
- 行高是动态的，可以被新插入的更高字形"撑高"
- `room_in_row` 不保证"行内剩余高度够不够"，因为行没有固定高度

### 4.3 为什么 room_in_row 不检查行内剩余高度？

因为行根本没有"固定高度"这个概念。行高是所有已插入字形高度的最大值，是**事后统计值**，不是事前设定值。

行装箱策略是：
1. 所有字形底部对齐 baseline
2. 谁最高，行高就由谁决定
3. 只要字形顶部不超出纹理顶部，就能放
4. 换行时，用最终的行高来定新 baseline

这种策略简单高效，但空间利用率取决于最高字形——一行里只要有一个特别高的字形，整行高度都被它拉高，其他字形上方全是浪费。

---

## 五、两次 room_in_row 的真实含义

insert() 中有两次 `room_in_row` 调用，中间夹着 `advance_row()`。很多人误以为：

> 第一次检查"当前行够不够"，不够就换行，第二次检查"新行够不够"。

这个理解**大体正确但不够精确**。更准确的描述是：

| 调用时机 | 检查的是什么 | 不检查什么 |
|---------|------------|----------|
| 第一次 `room_in_row` | 当前行宽度够不够 + 字形顶部会不会超出纹理顶 | 当前行已经有多高（row_tallest） |
| `advance_row` | 还有没有下一行的位置（`H - (B+T) > 0`） | 新字形能不能放进新行 |
| 第二次 `room_in_row` | 新行宽度够不够 + 字形顶部会不会超出纹理顶 | 新行最终会有多高 |

关键洞察：**两次 `room_in_row` 用的是同一份判定逻辑，只是 baseline 不同了**。高度判定永远是"字形顶部会不会伸出纹理顶"，跟"行内还剩多少空间"无关——因为行没有固定高度。

### 5.1 两次高度判定的差值

第一次高度阈值：`H - B`
第二次高度阈值：`H - (B + T)`

差值 = `T` = 当前行最高字形高度。

因为 `T >= 0`，所以第二次高度阈值 ≤ 第一次。换行后，高度检查**更严格**了。

### 5.2 一个反直觉的例子

`H=1024, B=0, T=800, gh=300`

- 第一次高度检查：`300 < 1024 - 0 = 1024` → ✅ 通过
- 假设宽度不够，触发换行
- 第二次高度检查：`300 < 1024 - 800 = 224` → ❌ 不通过 → Full

这个 300px 高的字形，在当前行能放下，但换行后反而放不下了。因为当前行的最高字形有 800px，换行后基线往上跳了 800px，剩下的空间只有 224px，不够放 300px。

但这不是 bug——这是行装箱策略的必然结果。上一行被最高字形（800px）"占满"了，下一行只能从 800px 以上开始。

---

## 六、行装箱策略的空间效率

### 6.1 理想情况：字形高度一致

如果所有字形高度相同（比如同一字体、同一字号的 ASCII 字符）：
- 行高 = 字形高度（无浪费）
- 空间利用率 ≈ 100%（除了最后一行可能没填满宽度）

这是终端字体的典型场景，效率很高。

### 6.2 极端情况：高度差异巨大

如果一行内既有很矮的字形（如标点符号，10px），又有很高的字形（如 emoji，40px）：
- 行高 = 40px
- 矮字形上方浪费 30px
- Y 方向利用率 = 10/40 = 25%

实际中没这么夸张，但混合 emoji/中文/英文的行，高度差异确实会造成一定浪费。

### 6.3 X 方向 vs Y 方向浪费对比

| 方向 | 浪费来源 | 能否避免 |
|:---:|---------|:------:|
| X 方向 | 最后一个字形后没填满一行 | 每行末尾少量，不可避免 |
| Y 方向 | 矮字形上方空白（行高取 max） | 行内高度差异越大浪费越多 |

终端场景下，X 方向浪费通常比 Y 方向小得多。因为一行可以塞很多字形（宽高比大），最后一个字形后剩下的宽度占比很小。

---

## 七、对缓存和渲染的影响

### 7.1 对缓存命中率的影响

行装箱策略本身不影响缓存命中率——字形一旦插入，就一直在纹理里，直到 `clear_atlas()`。

但 Atlas 的空间利用率会影响**图集满载频率**：
- 利用率高 → 单个 Atlas 能装更多字形 → 更少触发 Full → 更少创建新 Atlas
- 利用率低 → 更快装满 → 更多 Atlas → 更多 Batch 切换（因为 tex_id 变化）

### 7.2 对渲染批次的影响

每个字形的 `tex_id` 是它所在 Atlas 的纹理 ID。当渲染按 tex_id 分组时：
- 字形分散在多个 Atlas 中 → 更多 Batch 切换 → 更多 draw call
- 字形集中在少数 Atlas 中 → 更少 Batch 切换 → 渲染效率更高

所以行装箱效率间接影响渲染性能。

### 7.3 clear_atlas 后的复用

`clear_atlas()` 只重置 `row_baseline/row_extent/row_tallest`，不释放纹理。

清空后重新装填时，行高和行数取决于**插入顺序**。如果清空后插入顺序变化（比如先插了更高的字形），同样的字形集合可能占据不同的行数。但只要字形集合不变，总行数（总高度占用）的差异通常很小。

---

## 八、总结：三个容易错的直觉

| 直觉误区 | 代码事实 |
|---------|---------|
| "行有固定高度，room_in_row 检查剩余高度" | 行没有固定高度，room_in_row 只检查字形是否伸出纹理顶 |
| "换行后空间更多，字形更容易放下" | 换行后基线往上跳了 row_tallest，高度检查更严格了 |
| "空白浪费在字形下方" | 底部对齐，空白浪费在**矮字形上方** |
